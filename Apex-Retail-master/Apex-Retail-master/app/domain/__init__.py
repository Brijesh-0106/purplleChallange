"""Domain layer.

Pure Python — NO I/O, NO frameworks. This package holds entities and rules
that survive a rewrite of the API or the persistence layer. If you can't
unit-test it without a database, it doesn't belong here.
"""
